version: '3.9'

# Database is on Supabase — only local services here.

services:
  redis:
    image: redis:7-alpine
    container_name: clutch-redis
    ports:
      - '6379:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 5
